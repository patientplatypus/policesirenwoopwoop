// import { get } from 'lodash'

export default class BaseService {
  // getAuthToken = (req) => {
  //   return get(req, 'headers.Authorization', '') || get(req, 'headers.authorization', '')
  // }
  //
  // getId = (req) => {
  //   return get(req, 'params.id', '')
  // }
}
